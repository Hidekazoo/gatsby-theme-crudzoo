module.exports = ({
  basePath = "/",
  blogPath = "blog",
  blogsPath = "blogs",
  tagsPath = "tags",
  seriesPath = "series",
}) => ({
  basePath,
  blogPath,
  blogsPath,
  tagsPath,
  seriesPath,
})
